<%= form_tag(path, :method => "get", class: "form-horizontal" ) do %>
    <div class="col-sm-4">
      <%= text_field_tag(:title, nil, class: "form-control", placeholder: "Put title of movie ") %>
    </div>
    <%= submit_tag("Search", class: "btn btn-primary") %>
<% end %>